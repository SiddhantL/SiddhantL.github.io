{
  "name": "siddhant-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "sync-blog": "node --env-file=.env scripts/sync-notion-blog.mjs",
    "publish-blog": "node --env-file=.env scripts/sync-notion-blog.mjs && git add -A && git commit -m 'publish blog updates' && git push",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "astro": "^5.7.4",
    "notion-to-md": "^3.1.9"
  }
}
